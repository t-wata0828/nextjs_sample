"use client";

import { createTheme, ThemeOptions } from "@mui/material/styles";

const themeOptions: ThemeOptions = {
  // ここでテーマオプションをカスタマイズできます
};

const theme = createTheme(themeOptions);

export default theme;
